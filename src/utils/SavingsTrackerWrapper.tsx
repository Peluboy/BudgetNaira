// SavingsTrackerWrapper.tsx
import useSavingsTracker from '../hooks/useSavingsTracker';

const SavingsTrackerWrapper = () => {
  useSavingsTracker();
  return null;
};

export default SavingsTrackerWrapper;
